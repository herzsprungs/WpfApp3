<Page x:Class="WpfApp3.StudentViewBorrowBooksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp3"
      mc:Ignorable="d" 
       d:DesignHeight="450" d:DesignWidth="800"
      Title="StudentViewBorrowBooksPage"
      Background="#f5f5f5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Library Logo and Title -->
        <Border Grid.Row="0" Background="#3498db" Padding="20 15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📚" FontSize="28" VerticalAlignment="Center" Margin="0,0,15,0"/>
                <TextBlock Text="Campus Library" FontSize="24" FontWeight="SemiBold" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Welcome Message -->
        <Border Grid.Row="1" Background="White" Padding="20" Margin="0,2,0,0">
            <StackPanel>
                <TextBlock Name="WelcomeMessage" Text="My Borrowed Books" FontSize="22" FontWeight="Medium" Foreground="#2c3e50"/>
                <TextBlock Text="Manage your current borrowings and request new books" FontSize="14" Foreground="#7f8c8d" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <Border Grid.Row="2" Background="White" Margin="20,15" Padding="30" CornerRadius="8" BorderBrush="#e0e0e0" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Quote Section -->
                <Border Grid.Row="0" Background="#f9f9f9" Padding="15" CornerRadius="5" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="A reader lives a thousand lives before he dies. The man who never reads lives only one." 
                                   FontSize="16" FontStyle="Italic" TextWrapping="Wrap" Foreground="#34495e"/>
                        <TextBlock Text="- George R.R. Martin" FontSize="14" Foreground="#7f8c8d" 
                                   HorizontalAlignment="Right" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Current Borrowings DataGrid -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Current Borrowings" FontSize="16" FontWeight="Medium" Foreground="#2c3e50" Margin="0,0,0,10"/>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding BorrowedBooks}" 
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal"
                              AlternatingRowBackground="#f9f9f9"
                              BorderBrush="#bdc3c7" BorderThickness="1"
                              RowHeaderWidth="0">
                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#ecf0f1"/>
                                <Setter Property="Foreground" Value="#2c3e50"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#bdc3c7"/>
                            </Style>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Book Title" Binding="{Binding Title}" Width="*"/>
                            <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="0.6*"/>
                            <DataGridTextColumn Header="Borrowed Date" Binding="{Binding BorrowedDate}" Width="0.4*"/>
                            <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate}" Width="0.4*">
                                <DataGridTextColumn.CellStyle>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Padding" Value="10,8"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                                                <Setter Property="Foreground" Value="#e74c3c"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="Actions" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Renew" Background="#27ae60" Foreground="White" Padding="10,5" BorderThickness="0">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="4"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <!-- Request New Borrow Button -->
                <Button Grid.Row="2" Content="Request New Borrowing" 
                        Background="#3498db" Foreground="White"
                        Margin="0,20,0,0" Padding="15,8"
                        FontSize="14" FontWeight="Medium"
                        HorizontalAlignment="Left" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#34495e" Padding="15">
            <TextBlock Text="© 2025 Campus Library System" FontSize="12" Foreground="White" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Page>