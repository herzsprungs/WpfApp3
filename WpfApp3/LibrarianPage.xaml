<Window x:Class="WpfApp3.LibrarianPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp3"
        mc:Ignorable="d"
        Title="Librarian Dashboard" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Admin Navigation -->
        <StackPanel Grid.Column="0" Background="#607D8B">
            <TextBlock Text="Admin Panel" Foreground="White" FontSize="18" 
                       Margin="10" FontWeight="Bold"/>
            <Button Content="👥 Manage Users"  Click="NavButton_Click"/>
            <Button Content="🗃️ Full Database"  Click="NavButton_Click"/>
            <Button Content="⚠️ Remove Data"  Click="NavButton_Click"
                    Background="#D32F2F"/>
            <Button Content="💸 Fines Overview"  Click="NavButton_Click"/>
        </StackPanel>

        <!-- Content Area -->
        <Grid Grid.Column="1" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="ContentTitle" Text="Librarian Dashboard" FontSize="20" Margin="10"/>

            <Border Grid.Row="1" CornerRadius="5" Background="White" Padding="10"
               >
                <DataGrid x:Name="AdminGrid" AutoGenerateColumns="True"
                          CanUserAddRows="False" IsReadOnly="True"/>
            </Border>
        </Grid>

        <!-- Logout -->
        <Button Content="🚪 Logout" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="10" Padding="10 5" Click="btnLogout_Click"/>
    </Grid>
</Window>
