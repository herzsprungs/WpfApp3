<Window x:Class="WpfApp3.StudentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp3"
        mc:Ignorable="d"
        Title="Student Dashboard" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource PrimaryBrush}">
            <StackPanel>
                <Border Background="{StaticResource SecondaryBrush}" Padding="20">
                    <TextBlock Text="Student Portal" Foreground="White" FontSize="18" FontWeight="Bold"/>
                </Border>

                <Button Style="{StaticResource ModernButton}" Content="📚 Available Books" Margin="10" Click="NavButton_Click" Tag="AvailableBooks"/>
                <Button Style="{StaticResource ModernButton}" Content="📖 Borrow Books" Margin="10" Click="NavButton_Click" Tag="BorrowBooks"/>
                <Button Style="{StaticResource ModernButton}" Content="⏱️ Library Visits" Margin="10" Click="NavButton_Click" Tag="LibraryVisits"/>

                <Button Style="{StaticResource ModernButton}" Content="🚪 Logout" Margin="10 50 10 10" Click="Logout_Click"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <Frame x:Name="MainFrame" Grid.Column="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>