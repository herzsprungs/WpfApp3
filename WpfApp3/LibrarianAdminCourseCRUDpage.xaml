<Page x:Class="WpfApp3.LibrarianAdminCourseCRUDpage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp3"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LibrarianAdminCourseCRUDpage">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Add Course -->
        <Border Background="#F8F9FA" CornerRadius="5" Padding="10">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="txtCourseCode" Width="120"  Margin="5"/>
                <TextBox x:Name="txtCourseName" Width="300"  Margin="5"/>
                <Button Content="Add Course" Style="{StaticResource ModernButton}" Width="120"/>
            </StackPanel>
        </Border>

        <!-- Course List -->
        <DataGrid Grid.Row="1" Margin="0 10 0 0" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="1*"/>
                <DataGridTextColumn Header="Course Name" Binding="{Binding Name}" Width="3*"/>
                <DataGridTextColumn Header="Students" Binding="{Binding StudentCount}" Width="1*"/>
                <DataGridTemplateColumn Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" Style="{StaticResource GridActionButton}"/>
                                <Button Content="Delete" Style="{StaticResource GridActionButton}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>